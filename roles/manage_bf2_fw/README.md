# Update DPU Firmware Role
This role uses the Mellanox Firmware updater script to check for and install newer versions of firmware.

## Dependencies
* Internet access

## Defaults
* `force=False` - Firmware will not force apply

